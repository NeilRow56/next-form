// import { SignInButton, SignedIn, SignedOut, UserButton } from '@clerk/nextjs'

export default function Home() {
  return (
    <main className='container mx-auto flex min-w-0 gap-5'>
      <div className='w-full min-w-0 space-y-5'>
        {/* <SignedOut>
          <SignInButton />
        </SignedOut>
        <SignedIn>
          <UserButton />
        </SignedIn> */}
      </div>
    </main>
  )
}
